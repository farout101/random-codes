spring:
  application:
    name: framework_test_2
  # Database
  datasource:
    url: jdbc:mysql://localhost:3306/sales
    username: toot
    password: zinthu@mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  # JPA/Hibernate
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
      #  dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  # Thymeleaf
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
  # File upload
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true
# Server
server:
  port: 8080

